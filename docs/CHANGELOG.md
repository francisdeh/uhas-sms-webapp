# Changelog

Full development history for the UHAS SMS project, newest first. `README.md`'s Development Phases table is a slim index into this file — this is the canonical place for detailed "what shipped and why" narrative.

## Announcement notifications: email + SMS — ✅ Done

Fourth and final domain PR in the "close the email/SMS gaps, app-wide" initiative (Assignments → Schemes → Promotions → Announcements). Structurally the odd one out: every prior domain had a submit/review workflow with 2-3 directions (activity/decided), but Announcements is a single-event domain (`ANNOUNCEMENT_POSTED` only) whose real complexity is on the **audience** side, not the direction side — `all`/`division:X`/`class:X` scopes can resolve to staff, parents, or both at once, the first domain in this initiative to need that.

- **New `:staff` audience suffix** (`all:staff`, `division:JHS:staff`) — a genuine visibility distinction, not just a notification-channel filter. Confirmed with the user after they flagged a real gap ("maybe not every announcement needs to go to parents"): a staff-only post is invisible to parents in the in-app feed too, not merely un-emailed to them. `announcements/audience.py`'s `AllAudience`/`DivisionAudience` gained a `staff_only: bool` field threaded through create-time role gates, list-time visibility (`_parent_can_see`), and the notification fan-out — one source of truth for "who can see this" rather than separate see/notify logic. `class:X` has no staff variant by design (parent-only; class-scoped staff comms already go through assignments/attendance).
- **New `AllStaffAudience` resolver** (`notifications/audience.py`) — every active user whose role isn't Parent — the first genuinely new `AudienceSpec` variant added across this whole 4-PR initiative.
- **New reusable mixed-contact resolver** (`notifications/contacts.py`'s `resolve_user_contacts`) — every prior domain PR only ever needed one side of contact info (guardian-only for Assignments, staff-only for Leave/Schemes/Promotions); `division:X` (non-staff-only) is the first case needing both staff and parent contacts in one post, so this is a shared resolver rather than one more inline join.
- **One shared preference pair, not per-direction** (confirmed with the user) — `email_on_announcement_posted`/`sms_on_announcement_posted`, used identically regardless of recipient role, since there's no submit/review workflow to split directions over.
- **`is_critical` bypass semantics** (confirmed with the user): a critical post bypasses each recipient's own per-user preference but never the school-level `notification_defaults.on_announcement_posted` kill switch — that check happens once, upstream in `create()`, before the fan-out ever runs.
- **Batched SMS, per-recipient email** (confirmed with the user): unlike every prior domain's one-`sms/fanout.requested`-event-per-recipient, Announcements batches all recipients from one resolved audience spec into a single event via its `recipients` array — a school-wide "all" post can mean hundreds of guardians, and one Hubtel batch call beats hundreds of individual ones. Email stays per-recipient (Inngest's own concurrency handles arbitrary recipient counts without one job invocation looping through hundreds of provider calls).
- New `announcement_posted.html` email template with conditional critical-banner styling; new Inngest job; new preference rows added to all three role preference-row arrays in Profile → Notifications (Teacher, Parent, Admin/Deputy) since the pair is shared across every role.

Backend: `ruff check`/`ruff format --check`/`mypy`/`alembic upgrade head`/`pytest` all clean (967 passed, up from 943) — new tests cover the `:staff` suffix parser round-trip, staff-only list-visibility exclusion for parents, the mixed staff+parent contact resolver, the new `AllStaffAudience` resolver, batched-vs-per-recipient dispatch shape, the `is_critical` bypass (recipient opt-out vs. school-level toggle), independent per-channel opt-out, and the email job's critical/non-critical rendering. Frontend: `tsc`/lint/Vitest/build all clean; no drift between the regenerated OpenAPI types and the committed `api.d.ts`. Manually verified end-to-end as both Admin and Parent: posted a critical, staff-only announcement from the new audience dropdown option, confirmed the "Critical" badge and "All school (staff only)" label render correctly and the in-app bell count incremented for Admin, then confirmed as Parent that the same post is genuinely absent from their announcements feed and that both roles' Profile → Notifications tabs show the new Email/SMS — Announcements rows correctly.

## Promotion notifications: email + SMS — ✅ Done

Third of four domain PRs extending in-app-only notifications with email + SMS. The biggest of the three so far — Promotions already had 5 in-app notification kinds across 3 distinct recipient shapes, not 2: `PROMOTION_SEASON_OPENED` (a mass broadcast to **every teacher in the school**, fired when Admin opens the season — infrequent, ~1-2×/year at term boundaries), `PROMOTION_SUBMITTED` (reviewer-facing: Deputy Head of the class's division + all Admins, same dual-eligibility shape as Leave Requests), and `PROMOTION_SENT_BACK`/`PROMOTION_APPROVED`/`PROMOTION_REMINDER` (all teacher-facing, about the teacher's own submission — an outcome or a nudge to submit).

- New `_notify_promotion_channels`/`_notify_promotion_season_opened`/`_notify_promotion_reviewers`/`_notify_promotion_teacher` helpers in `promotions/service.py`, wired into `open_season`, `submit_list`, `send_back`, `approve`, and the weekly `send_unsubmitted_reminders` job.
- **Three preference pairs, not two** — confirmed with the user before implementing: `on_promotion_season` (all-teachers broadcast gets its own toggle, since its audience is entirely different from the other two directions), `on_promotion_activity` (reviewer-facing on submit), `on_promotion_decided` (teacher-facing, folding sent-back + approved + reminder into one bucket — same "one direction per audience, not per event" pattern Schemes established).
- **Confirmed with the user**: the season-opened broadcast gets email + SMS too, despite being a mass send — infrequent enough (once or twice a year) that the cost/spam concern of a school-wide blast doesn't outweigh staying consistent with every other event in this initiative.
- New `"promotion"` SMS category; 5 new email templates/jobs (one per notification kind, since — unlike Schemes' shared "commented" template — each of these 5 events has genuinely distinct content). New preference rows: Teacher gets season + decided; Admin/DeputyHead get activity (the shared preference-row array serving that pair was renamed from `LEAVE_APPROVER_PREFERENCE_ROWS` to `ADMIN_DEPUTY_PREFERENCE_ROWS`, since it now also carries promotion-activity rows, not just leave ones).
- **Also switched the frontend dev server from Webpack to Turbopack** (`next dev --turbopack`) — unrelated to promotions, but found and fixed in the same session: `--webpack` had been the scaffold default since the project's first commit, never a deliberate choice, and was making local dev noticeably slower to compile across this app's large route count. Verified clean across login, dashboard, settings, and profile pages (including the Sentry-instrumented `next.config.ts`, which only applies at build/production time and doesn't conflict with the Turbopack dev path).

Backend: `ruff check`/`ruff format --check`/`mypy`/`pytest` all clean (943 passed) — 26 new tests cover the all-teachers broadcast, the reviewer dual-eligibility audience, all 3 directions' two-tier gating independently, and all 5 email jobs' send-provider contract. Frontend: `tsc`/lint/Vitest/build all clean; no drift between the regenerated OpenAPI types and the committed `api.d.ts`. Manually verified the preference UI end-to-end for both Teacher and Admin roles — the live submit/approve/send-back/reminder flows were left to the (already-thorough) automated test suite rather than re-exercised against the shared local dev database, which already has real submitted/approved promotion data from earlier manual testing this project.

## Scheme notifications: email + SMS — ✅ Done

Second of four domain PRs extending the in-app-only notification pattern with email + SMS. Structurally different from Assignments (PR 8, single-direction, guardian recipients) — Schemes already had **three** in-app-only notification kinds across **two directions**: `SCHEME_SUBMITTED` (teacher submits → notifies Unit Heads of the class's division) and `SCHEME_COMMENTED`/`SCHEME_ACKNOWLEDGED` split bidirectionally (a teacher's comment nudges Unit Heads; a reviewer's comment or acknowledgement nudges the submitting teacher). That two-direction, staff-recipient shape is exactly what `LeaveRequestsService` (Phase 6 item 8 PR 3) already solved, so this PR mirrors `_notify_leave_channels`/`_notify_leave_approvers`/`_notify_leave_requester` almost directly rather than the Assignments/Attendance guardian-fan-out shape.

- New `_notify_scheme_channels` helper (`schemes/service.py`) — the shared email+SMS gate, direction-parameterized (`"activity"` = Unit-Head-facing, `"decided"` = teacher-facing) — plus `_notify_scheme_unit_heads` (resolves every Unit Head of a division via the existing `UnitHeadOfDivisionAudience`, joins `Staff` for phone numbers) and `_notify_scheme_teacher` (single recipient, mirrors `_notify_leave_requester`). Wired into all three call sites: `submit()`, `acknowledge()`, and both branches of `add_comment()`.
- **Deliberate scope decision, confirmed with the user before implementing**: `SCHEME_SUBMITTED`'s audience stays Unit-Head-only — no Deputy Head/Admin fallback for a division with no Unit Head (unlike Leave Requests, which always includes Admins). Widening that audience was treated as a separate decision from "add email/SMS channels to whoever already gets notified today."
- Two preference pairs, not four (also confirmed with the user) — `on_scheme_activity`/`on_scheme_decided` at the school level, `{email,sms}_on_scheme_activity`/`{email,sms}_on_scheme_decided` per user (new migration) — matching Leave Requests' by-direction grain rather than one toggle per event.
- New `"scheme"` SMS category; 3 new email templates + Inngest jobs (`scheme_submitted`, `scheme_acknowledged`, `scheme_commented` — the last shared by both comment directions, since the send-what-I'm-told job doesn't care who commented). New preference rows in the Teacher's Profile → Notifications tab (shown to every Teacher, same as the existing appointment/lesson-plan rows — no `is_unit_head` gating in this UI, consistent with precedent) and a new "Scheme submitted/acknowledged" pair in Admin's Settings → Communication tab.
- **Fixed misleading copy found during manual verification**: the scheme submit button read "Submit to Head of School" (and a matching line on the schemes list page) — but the actual recipient is the Unit Head only, never the school's Head/Principal (a distinct, unrelated concept used correctly elsewhere for report-card signatures). Changed to "Submit for review" in both spots, which stays accurate regardless of whether a Unit Head, Deputy Head, or Admin ends up acting on it.

Backend: `ruff check`/`ruff format --check`/`mypy`/`pytest` all clean (926 passed) — 15 new tests cover the Unit-Head-only audience (including the no-Unit-Head-in-division silent case), both directions' two-tier gating independently, the bidirectional comment dispatch (teacher comment → Unit Head, reviewer comment → teacher), and all 3 email jobs' send-provider contract. Frontend: `tsc`/lint/Vitest/build all clean; no drift between the regenerated OpenAPI types and the committed `api.d.ts`. Manually verified end-to-end: submitted a real scheme as the Teacher demo account, confirmed the Unit Head demo account received the in-app notification (email/SMS Inngest events correctly enqueued with the right payload — the local Inngest dev server's stale SDK-URL connection prevented them from executing this session, a pre-existing environment quirk unrelated to this PR, not a code defect), and confirmed the new preference rows render and persist correctly for both the Teacher and Admin roles. One leftover notification row this manual pass committed to the shared local dev database was cleaned up afterward — the standing dev-DB-pollution risk this project already tracks (unscoped test queries can pick up rows a live browser session commits outside any test transaction).

## Assignment notifications: email + SMS — ✅ Done

First of four domain PRs extending the existing in-app-only notification pattern with email + SMS (Assignments → Schemes → Promotions → Announcements, in that order) — Assignments picked first as the most bounded. Before this PR, publishing an assignment fired only an in-app `ASSIGNMENT_CREATED` notification via `NotificationsService.notify_audience(ParentsOfClassAudience(...))`; that resolver only returns bare `users.id`s, insufficient for email/SMS which need the guardian's phone/email, so — mirroring how `AttendanceService._notify_attendance_absences` solved the identical problem — a new dedicated `AssignmentsRepository.list_primary_guardians_for_class` query (joining active-enrollment students → primary guardian → guardian's linked app user, scoped by class + academic year) replaces the generic audience resolver entirely in `AssignmentsService.publish()`.

- New `_notify_assignment_created` helper (`assignments/service.py`) fans out to every primary guardian of an actively-enrolled student in the published class: one in-app notification, one email (`email/assignment-created.requested` → new `assignment_created.html` template + Inngest job), and one SMS (via the existing shared `sms/fanout.requested` event) per guardian — deduped so a guardian with two children in the same class gets one message, not two.
- Two-tier gate, same shape as every other domain: school-level `notification_defaults.on_assignment_created` (defaults **true**, unlike attendance's deliberate opt-out — an assignment-posted event doesn't share attendance's daily-volume/sensitivity profile) plus per-user `user_preferences.email_on_assignment_created`/`sms_on_assignment_created` (new migration, both default true).
- New `"assignment"` SMS category added to the closed `SmsCategory` set (`sms/constants.py`) — one category per notification domain is the established pattern; `sms_log.category` is a plain `String(30)` column, no DB enum constraint, so this needed no migration.
- Frontend: two new preference rows ("Email/SMS — New Assignments") in the Parent's Profile → Notifications tab, and a new "Assignment published" toggle in Admin's Settings → Communication tab — both wired through the existing `PreferenceField` union / `NotificationDefaults` type, no new components.

Backend: `ruff check`/`ruff format --check`/`mypy`/`pytest` all clean (911 passed) — new tests cover school-level gating (default-on, explicit opt-out), per-user email/SMS gating independently, guardian dedup (two children, one class), class-scoping (a guardian's child in a different class doesn't get notified), the no-guardian-on-file case, and the email job's send-provider contract. Frontend: `tsc`/lint/Vitest/build all clean — surfaced and fixed 3 downstream type errors from the regenerated OpenAPI types (`SessionUser`, `CommunicationTab`, and the hand-written `NotificationDefaults` type in `features/settings/types.ts` all needed the new fields). Manually verified end-to-end: published a real assignment as the Teacher demo account, confirmed Draft → Published transition and the new preference rows render and persist correctly for both the Parent and Admin roles.

## Supabase Send SMS hook — relay phone-OTP through Hubtel — ✅ Done

Second of two provider-wiring PRs, deliberately kept separate from the first (Brevo/Hubtel precedence swap) since this one adds a genuinely new piece — a public, unauthenticated webhook endpoint — rather than a simple factory-precedence change.

**The gap**: Supabase Auth's phone-OTP sign-in (the Parent login path) has always used a placeholder `[auth.sms.twilio]` block in `supabase/config.toml` with unset credentials, existing only to satisfy Supabase's "at least one provider must be enabled" requirement — a comment in that file even flagged this as "Phase 3 — not implemented here" months ago. Real OTP delivery never actually worked; only the pinned `test_otp` numbers did.

**The fix**: a new `POST /auth/send-sms-hook` endpoint (`app/features/auth/router.py`) registered as Supabase's custom **Send SMS hook** — Supabase now calls this instead of Twilio whenever it needs to deliver an OTP, and the endpoint relays the send through the app's own `get_sms_provider()` (Hubtel, per the previous PR). Every request is verified against the Standard Webhooks HMAC signature (new `standardwebhooks` dependency — the same open spec Supabase itself implements, not a Supabase-specific SDK) using a shared secret Supabase generates when you register the hook. Reads the **raw** request body before any JSON parsing, since signature verification must be over the exact bytes sent, not a re-serialized version.

**Deliberately fails closed**: unlike every other provider setting in this codebase ("missing config isn't an error, just skip"), a missing `SEND_SMS_HOOK_SECRET` here returns a 500 rather than silently accepting unsigned requests — this is the one place where "safe to skip" would actually mean "anyone who finds the URL can relay arbitrary SMS through the school's Hubtel account."

**Scope decision**: hook-triggered OTP sends do *not* get logged to `sms_log` like every other domain's SMS (fee reminders, leave notifications). Supabase's hook payload may not yet include our own custom `app_metadata.school_id` claim on a brand-new signup (it only reliably has Supabase's own `provider`/`providers` fields at that point), which would make `school_id` resolution unreliable for logging purposes — Hubtel's own dashboard already covers delivery/cost auditing for this flow, so the endpoint calls the provider directly rather than routing through `SmsService.send()`'s DB-logging wrapper.

**Testable locally, not just in production**: Supabase's local Auth container (running in Docker) reaches the FastAPI dev server via `http://host.docker.internal:8000/auth/send-sms-hook` on macOS — new `[auth.hook.send_sms]` block in `supabase/config.toml`. Real Hubtel OTP delivery can be exercised from a developer's own machine the same way production will.

Backend: `ruff check`/`ruff format --check`/`mypy`/`pytest` all clean (899 passed). New tests build real, validly-signed webhook requests using the same `standardwebhooks` library the endpoint verifies against (`Webhook.sign`), rather than mocking verification away — covering valid relay, wrong signature, signature from a different secret, unconfigured secret (fails closed), provider-reported failure, and malformed payloads. The endpoint is excluded from the OpenAPI schema (`include_in_schema=False`, confirmed via a fresh `/openapi.json` diff) — Supabase is the only caller, so there's no reason for it to appear in the frontend's generated API client.

## Brevo email + Hubtel SMS provider wiring — ✅ Done

First of two PRs kicking off an "email/SMS delivery gaps" initiative (Announcements/Assignments/Promotions/Schemes each still lack email or SMS — see the migration plan's item 8). This PR is the foundational piece: making the school's actually-chosen providers the real, primary ones, since every domain PR after this one will send through whichever provider is active.

- **Brevo replaces Resend as the email provider.** `ResendEmailProvider` was never configured with real production credentials — the school has now set up a real Brevo account, so `ResendEmailProvider` and `resend_api_key` were removed entirely rather than kept as unused dead code, and a new `BrevoEmailProvider` (Brevo's transactional email API, `POST /v3/smtp/email`, `api-key` header auth) takes its place as the primary provider in `get_email_provider()`'s precedence, ahead of the existing SMTP/Mailpit fallback. New `brevo_api_key`/`brevo_sender_email`/`brevo_sender_name` settings, mirroring the existing Arkesel/Hubtel settings' shape (dedicated fields, not a reused "Name <email>" string).
- **Hubtel becomes the primary SMS provider, Arkesel the fallback** — a one-line precedence swap in `get_sms_provider()`. `HubtelSmsProvider` was already fully implemented (built during an earlier Phase 5 fee-reminder PR) but sat behind Arkesel; the school has now settled on Hubtel as its chosen provider.
- Both providers can be tested locally, not just in production — the existing provider-factory pattern (env var presence resolves which provider loads, in any environment) already supports this; setting real `BREVO_API_KEY`/`HUBTEL_*` values in a local `.env` sends through the real providers from a developer's own machine, same as production.
- Fixed 3 places where documentation had gone stale on this exact subsystem: `docs/DEPLOY.md`'s SMS/email sections still described Hubtel as "not yet live" and email as "SMTP today, swappable to Resend/SendGrid/Postmark later" (both written before either provider existed in code); `apps/api/README.md`'s Phase 3 status table still listed Hubtel as "interface + stub exist, needs an account first."

Backend: `ruff check`/`ruff format --check`/`mypy`/`pytest` all clean (892 passed). New `respx`-mocked tests cover `BrevoEmailProvider`'s request/response contract (success, HTTP error, exact request shape) — the original Resend tests only covered factory resolution, not the provider's actual send behavior, so this is stricter coverage than what it replaced. Provider-precedence tests updated for the new Hubtel-primary order.

## Promotions revisit — ✅ Done

Closed the last of Phase 6's original two vague gap-audit items ("promotions revisit," previously undefined beyond its name — the other, "search navigation revisit," shipped earlier). Two fresh research passes — one auditing the feature for the same categories of issue found in past revisits (dead routes, dropped fields, navigation bugs, missing overviews), one comparing its depth against the more mature Lesson Plans/Schemes review flows — surfaced a genuinely large backlog for a feature that had never gotten a dedicated look:

- **Notifications were half-wired**: `submit_list()` never notified the Deputy Head of a new submission; `approve()` never notified the teacher of the outcome (only `send_back` did). Added `PROMOTION_SUBMITTED`/`PROMOTION_APPROVED` in-app notifications — submit fans out to every Deputy Head of the class's division plus every Admin (mirroring `LeaveRequestsService`'s dual-eligible-approver audience exactly), approve notifies the submitting teacher. In-app only, deliberately — promotions predates the email/SMS notification initiative (5 PRs earlier this project, covering appointments/leave/attendance/auth), so this matches the existing `PROMOTION_SEASON_OPENED`/`PROMOTION_SENT_BACK` precedent rather than scope-creeping into a new channel.
- **Comment history was lost on repeat send-backs**: `reviewer_comment` was a single overwritable column — a second send-back erased the first comment. New `promotion_comments` append-only thread table, mirroring `scheme_comments` (which solved the identical problem for Schemes) field-for-field, migration included (backfills the existing single comment as history, drops the column). A new `PromotionCommentThread` component renders the full thread on the Admin/DeputyHead detail pages and the Teacher's own class page, replacing the single-comment alert box.
- **No deadline/reminder** for teachers who hadn't submitted before the season closes. New weekly Inngest job (`promotion-season-reminder-weekly`, Mondays 07:00 — same slot as the fee reminder), mirroring `FeesService.send_overdue_reminders`'s shape: sweeps every open-season school, reminds class teachers of any class not yet submitted/approved, auto-creating a submission row via the existing idempotent `ensure_submission` for classes the teacher never opened, with a 6-day cooldown stamp so a class isn't reminded twice in the same week.
- **No bulk-approve** — a Deputy Head had to review and approve each class individually. New `POST /promotions/submissions/bulk-approve`, best-effort: each submission is attempted independently inside a `session.begin_nested()` savepoint, so one bad row (wrong division, missing target class) rolls back only that submission, not the whole batch. New `PromotionReviewQueue` client component on the DH queue page adds per-row checkboxes (only on "submitted" rows), a "select all pending" toggle, and an "Approve N selected" confirmation flow.
- **Admin's exam-published warning was permanently wrong**: the page had its own `// GAP:` comment admitting `hasPublishedTerm3EndOfTerm` was hardcoded to `false` because no endpoint exposed it — meaning the "Term-3 exam isn't published yet" warning showed even when the exam *was* published. Fixed with a new standalone `GET /promotions/term3-exam-status` endpoint (deliberately separate from `GET /promotions/season`, which returns `null` before a season ever opens — exactly when this flag matters most for the "you'll need override" warning).
- **Small fixes along the way**: the "promotions" breadcrumb segment rendered as a raw lowercase path (missing from `AutoBreadcrumb.tsx`'s label map, unlike every sibling feature); 4 missing closed-season rejection tests (`save_draft`/`submit_list`/`send_back`/`approve` were gated identically to `ensure_submission` but only that one had a test).

**Two more bugs found and fixed during manual verification, beyond the above scope**: Admin's `/admin/promotions` overview grid computed each class's primary teacher (`const primary = ...`) but only ever used it in a negation (`{!primary && <Badge>No class teacher</Badge>}`) — the name itself was never rendered, the exact "computed but dropped" bug class the earlier audit-cleanup PR fixed elsewhere. And a real, pre-existing bug (not introduced by this PR): manually switching a decision to "Repeat" in `PromotionDecisionTable.tsx` always cleared `targetClassId` to `null` — only "Promote" gets a class-picker — and `submit_list()`'s pre-flight only validated a target class for Promote, not Repeat, so a Repeat decision would submit successfully only to crash at approve time with "no target class." Fixed at the root: a repeat's target class isn't a real choice (it's mechanically "same class name, next year," the same logic `_ensure_decisions_for_roster` already uses for the algorithmic suggestion) — `_apply_decision_updates` now auto-derives it server-side whenever missing, and `submit_list` gained a matching pre-flight check (mirroring Promote's) for the case where no next-year class exists to derive from, so that failure surfaces clearly at submit time instead of confusingly at approve time.

Backend: `ruff check`/`ruff format --check`/`mypy`/`pytest` all clean (889 passed — the 2 pre-existing SMTP-config test failures from this machine's local `.env` were also fixed in passing, by making them explicitly monkeypatch `smtp_host` to `None` rather than relying on it being unset in the ambient environment). Frontend: `tsc`/lint/Vitest/build all clean. Manually verified end-to-end across Admin, Teacher, and DeputyHead roles in two full browser passes — the second specifically re-confirming the two bugs found during the first were actually fixed (a repeat decision now submits and approves cleanly end-to-end; a class's teacher name now shows on the Admin overview).

## Audit findings: promotions + attendance revisit — ✅ Done

Second and final PR closing out the "medium/low-severity audit findings" backlog item (the first — dead-code cleanup + missing edit UIs — shipped as PR #80). Bundles the two remaining clusters the user chose to ship together: attendance's unwired Excused status, and promotions' unused class-teacher data. Pure frontend — zero backend files touched, since the backend already fully supported both features end-to-end.

**Attendance Excused status** — the backend has always supported 4 statuses (`Present`/`Absent`/`Late`/`Excused`, enforced via a Pydantic `Literal` in `attendance/constants.py`), but the frontend's `AttendanceStatus` type only had 3 — Excused was fully valid and persisted correctly by the API but had zero path to be set from the marking UI. Worse: `admin/attendance/[classId]/page.tsx`'s `API_TO_UI_STATUS` map explicitly mapped `Excused → "absent"`, silently downgrading any existing Excused record to Absent on display, and re-saving that class would persist the lossy conversion — a real, reproducible data-corruption bug, confirmed against pre-existing seeded Excused records that had never round-tripped correctly. Fixed by centralizing `ATTENDANCE_STATUS` (mirroring the established `EXAM_TYPE`/`SUBJECT_CATEGORY` pattern), adding Excused as a 4th button in `AttendanceSheet.tsx` with its own color, and fixing the lossy map. No reason-capture UI for Excused (unlike Late, which has a required `late_reason` — the backend has no generic/excused-specific reason column, and adding one would need a migration outside this PR's scope). `ParentAttendanceView.tsx`'s `STATUS_DOT` map required one forced minimal addition (TypeScript's `Record<AttendanceStatus, string>` doesn't compile with a member missing) — its stat cards and legend row were deliberately left untouched as out of scope. Verified end-to-end: marked a student Excused, saved, reloaded, and confirmed via direct Postgres query that the row persists as `Excused` (not silently coerced), both for a fresh save and for pre-existing seeded records from 2026-01-19.

**Promotions class-teacher display** — `SubmissionDetail.classTeachers` and `DeputyHeadQueueRow.classTeacherNames` were already computed via an extra join on every submission-detail and DH-queue-list call, but silently dropped by every frontend consumer. Added a "Class teacher(s): ..." line to the Admin and DeputyHead submission-detail pages and each row in the DH queue list, mirroring the existing display pattern in `teacher/department/page.tsx`.

**Bug found and fixed during manual verification (not part of the original scope, but directly in a file already being edited)**: `deputy-head/promotions/[submissionId]/page.tsx` crashed for *any* submission with decisions — a hard, pre-existing bug that had apparently never been exercised before, since this was the first live promotion submission ever created in the local dev database this session. Root cause: the page passed the raw flat `DecisionRead[]` response straight into `PromotionDecisionTable` via an unsafe `initial={detail.decisions as unknown as DecisionRowView[]}` cast, but the component expects a nested `{decision: {...}, studentName, studentPhotoUrl}` shape — the sibling Admin page has always transformed this correctly, DeputyHead's never did. Every row's `decision` field silently resolved to `undefined` (since JS allows indexing into a string without throwing), which broke the decision-icon lookup (`Element type is invalid... got undefined`) and the row keys (`row.studentId` also `undefined` for every row, tripping React's duplicate-key warning). Because the crash happened during React's render phase, `useBreadcrumbLabel`'s effect never committed either, leaving the raw submission UUID in the breadcrumb instead of the class name — reported independently by the user before the root cause was traced. Fixed by mirroring Admin's existing correct transformation. Verified by opening a real promotion season, submitting a live promotion list as the Teacher demo account, and reviewing it as both Admin and DeputyHead — both now render cleanly with 0 console errors and the correct breadcrumb label.

Frontend: `tsc`/lint/Vitest/build all clean. No backend changes, so no backend verification needed. Manually verified in-browser across Admin, Teacher, and DeputyHead roles.

## Audit findings: cleanup + missing edit UIs — ✅ Done

First of two PRs closing out the "medium/low-severity audit findings" backlog item (the second — promotions review data reuse + attendance Excused/Late status — is still open). Three parallel research agents audited the codebase for unwired enum values, dead backend endpoints with no frontend consumer, and response fields the frontend silently drops. This PR bundles the two clusters the user chose to ship together: dead-code cleanup and missing edit UIs.

**Dead-code cleanup** — four confirmed-dead routes deleted after re-verifying with a fresh grep immediately before removal (a router endpoint can look dead while its underlying service method is still used internally by a sibling endpoint — this happened repeatedly and the internal callers were kept):
- Standalone guardian CRUD (`POST /guardians`, `GET /guardians/{id}`) — superseded by the student-scoped add/update flow already in use everywhere. `PATCH /guardians/{id}` was initially flagged alongside these but turned out to have zero callers *and* real, working logic (syncing a guardian's phone/email to Supabase Auth) — a genuinely missing UI, not dead code, so it moved to the edit-UI list below instead of being deleted.
- `GET /students/{id}/guardian` (singular "first guardian" convenience route) — superseded by the plural `guardians()` call used everywhere.
- `GET /enrollments/{id}` and `GET /fees/learner-fees/{id}` — both had zero standalone callers; their list-scoped siblings cover every display site.
- Test fixtures that used the deleted `POST /guardians` purely as setup scaffolding were reworked: a new `_seed_guardian()` helper inserts the SQLAlchemy model directly, and `test_staff_as_guardian.py` was rewritten to route through the surviving `POST /students/{id}/guardians` flow instead.
- One candidate, `POST /notifications/mark-read` (per-item mark-read), was also initially flagged as dead — it wasn't. The real bug was `NotificationsDropdown.tsx` marking *every* notification read the instant the bell dropdown opened, which made the per-item route look unreachable. Fixed the actual UX instead of deleting a needed endpoint: removed the mark-all-on-open behavior, added per-item mark-read on click, kept the explicit "Mark all as read" button as the only bulk-read path.
- `GET /class-subjects`'s unused by-subject query mode was left alone — it's a branch of a shared endpoint, not a separate route, so surgically removing it would add risk for no real payoff.

**Missing edit UIs** — four domains had a working `PATCH` endpoint and an already-built-but-unused TanStack Query hook, but no edit UI: Exam, Class, Fee Item, and Subject each got a new Edit dialog (shadcn Dialog + react-hook-form + Zod, mirroring each domain's existing create-dialog field set). A fifth was added mid-implementation once the guardian PATCH route turned out to be a real gap: a small "Edit contact info" dialog on `GuardianTab.tsx` (first/last name, phone, email — reusing the same backend logic that syncs changes to Supabase Auth). Drive-by fix bundled in: the Subject category dropdown (both create and the new edit dialog) was missing the `Optional` category value that the backend has always supported — `SUBJECT_CATEGORY`/`SUBJECT_CATEGORIES` constants added to `features/classes/types.ts` and swept through.

**Bug found during manual verification, fixed in the same PR**: the "Mark all as read" button didn't persist — clicking it left the badge count unchanged even after a full reload. Root cause: `DropdownMenuItem` in this codebase wraps **Base UI's** `Menu.Item` (`@base-ui/react/menu`), not Radix — confirmed directly from Base UI's shipped type definitions, which expose `onClick` but no `onSelect` prop at all. The button's handler was wired as `onSelect={(e) => { e.preventDefault(); markAll.mutate(); }}`, a leftover Radix-shaped handler that Base UI's `Menu.Item` never invokes, so the mutation never fired. Reproduced directly against the local Postgres instance (the row stayed `read_at = NULL` after clicking, while the already-`onClick`-based per-item mark-read correctly flipped its row) before fixing it to a plain `onClick={() => markAll.mutate()}`, matching the pattern already proven to work for individual items in the same file. Also fixed three stale docstrings/comments elsewhere in the notifications feature that still described the old (removed) "mark-all on dropdown open" behavior.

**Second self-caught regression, same anti-pattern this PR was cleaning up**: the new Subject edit dialog reintroduced the exact hardcoded-literal pattern an earlier PR this cycle had swept away — `subject.category ?? "Core"` appeared four times in `SubjectsTable.tsx` instead of using the just-added `SUBJECT_CATEGORY` constant, and `CATEGORY_PILL` was typed `Record<string, string>` instead of `Record<SubjectCategory, string>` (the sibling `DIVISION_PILL` right above it already uses the precise `Record<Division, string>` form). Fixed all four call sites plus the map's type. This surfaced a second, pre-existing instance of the same category of debt: `SelectItem value="1">Term 1"` etc. was hardcoded across **five** sites (`ExamsManager.tsx` create + edit, `SchemeForm.tsx`, `LessonPlanForm.tsx`, `FeeItemForm.tsx`) with no shared constant — `term` was only ever typed `z.number().int().min(1).max(3)`. Added `TERMS: readonly number[] = [1, 2, 3]` to `features/exams/types.ts` (exams is the natural owner — the school's 3-term structure is an exam-period concept) and swept all five sites, plus a sixth latent instance (`CalendarTab.tsx`'s `seedTerms` helper, `[1, 2, 3].map(...)`) to `TERMS.map(...)`. `FeeItemForm.tsx`'s hybrid `"annual" | "1" | "2" | "3"` field kept its own `"annual"` sentinel item and only reused `TERMS` for the three numeric options — the two concepts don't share a shape, so no forced unification.

**Third finding, this one a real authorization gap, not a literal**: investigating whether the new Guardian edit dialog handled phone-as-login-data correctly (it does — `GuardiansService.update()` deliberately syncs phone/email straight to Supabase Auth for Admin, no OTP challenge, an existing documented trust decision) surfaced that DeputyHead's student-profile page shows the *entire* edit surface — Edit info, Transfer Class, and all guardian actions — despite every one of those backend routes (`update_student`, enrollment create/change-status, the 5 guardian-mutation routes) being `RequireAdmin`-only. `StudentDetail.tsx`'s `canEdit` prop defaults to `true`, and both the Admin and DeputyHead pages render it without passing the prop — only the Teacher page opts out. Initially looked like a backend gap (an established `RequireAdminOrDeputy` + division-scoped-check pattern already exists in `ClassesService`/`lesson_plans`/`schemes`/`promotions` for exactly this shape of problem), but `students/service.py`'s own `update_medical` docstring settled it the other way: DeputyHead's division scope was deliberately designed to cover *reads* only (`_assert_can_view_student` has the division carve-out) — *writes* to student core fields, guardians, and medical info are intentionally Admin-only, on purpose, "matching every other student-record mutation in this feature." Fixed the actual mismatch: `deputy-head/students/[id]/page.tsx` now passes `canEdit={false}`, matching the Teacher page's existing pattern — DeputyHead becomes read-only for student profiles (guardians included), consistent with what the backend already enforced.

Backend: `ruff check`/`ruff format --check`/`mypy`/`pytest` all clean (866 passed, same 2 pre-existing unrelated SMTP-config failures from this machine's local `.env`) — no backend files touched by the Term/DeputyHead fixes. Frontend: `tsc`/lint/Vitest/build all clean — one lint warning surfaced along the way (React Compiler flagging `react-hook-form`'s `watch()` as unmemoizable) fixed by switching the Fee Item edit dialog's Active toggle to `Controller`, the pattern already used elsewhere in the codebase for non-native form inputs. Manually verified in-browser: all 5 edit dialogs open pre-filled, save correctly (including a live end-to-end Guardian contact-info save + Supabase Auth phone-sync + revert, tested as Admin), and reflect immediately; Fee Item edit dialog re-verified as Accountant; the Subject category fix confirmed in both create and edit dialogs; the notification per-item/mark-all behavior confirmed via direct database inspection before and after the fix; DeputyHead's student profile confirmed to show only "Print ID Card" and a read-only Guardian tab after the `canEdit={false}` fix. See `docs/superpowers/specs/2026-07-14-audit-cleanup-edit-uis-design.md`.

## Search navigation revisit — ✅ Done

Closed the last of the two remaining vague backlog items from Phase 6's original gap audit (the other, "promotions revisit," is still open). A two-round research pass on the existing search palette (`SearchCommand.tsx`, backed by a real role-scoped `GET /search` endpoint — not a hardcoded client list) found: a fully-built but dead "Announcements" results branch (hardcoded to `[]`, no backend field); real navigation bugs where staff/student search hits fell back to a role's dashboard root instead of a real detail page; and only 3 of 6 plausible searchable domains wired up (students/staff/classes, not fee items/lesson plans/schemes).

Fixing the navigation bugs surfaced two genuine, backend-already-permitted-but-UI-absent capability gaps: **DeputyHead had no staff detail page**, and **Teacher had no student detail page** — both `GET /staff/{id}`/`GET /students/{id}` already allowed these roles, just with no frontend route. Admin/DeputyHead's "missing" lesson-plan/scheme detail pages turned out not to be real gaps — full review UIs (`ReviewQueue`, `LessonPlansOversight`, `AdminSchemeReview`) already exist as expand-in-place lists; they just needed deep-link support.

Shipped as one PR, scoped up from an initially narrower plan at the user's direction:
- **Dead code removed**: the Announcements search branch.
- **Backend permission widenings**: `_assert_can_view_student` gained the same Teacher class-teach-or-subject-teach check `_assert_can_view_medical` already had (guardians/siblings/documents sub-resources); `StaffService.list_documents` gained a DeputyHead-same-division branch (previously Admin-or-self only).
- **Two new detail pages**: `/teacher/students/[id]` (read-only — no Edit/Transfer, no Guardian mutation controls) and `/deputy-head/staff/[id]` (read-only — no Edit Info/Deactivate/Access tab), both wired as real search-result destinations and as click targets on existing entry points (the teacher attendance roster, the DeputyHead dashboard's "Staff in Division" list).
- **Search expanded** to fee items (Accountant only, school-wide), lesson plans and schemes (Admin unrestricted, DeputyHead/Teacher division- or own-scoped) — new `SearchRepository` methods + `SearchResults` schema fields, with a new Accountant branch in `SearchService` that previously fell through to an empty result.
- **A `?focus=<id>` deep-link mechanism** for the expand-in-place review pages: a search hit for a lesson plan/scheme now lands on `/admin/lesson-plans?focus=<id>` (etc.), pre-opening and scrolling to the right card.
- **Two new list pages, added mid-implementation**: fixing the two new detail pages' backend gaps left DeputyHead and Teacher with a detail route but no list route — `AutoBreadcrumb.tsx` builds breadcrumb links by mechanically joining URL segments with no check that the intermediate path resolves to a real page, so both new detail pages' breadcrumbs pointed at a 404. Rather than patch the breadcrumb, built the missing list pages properly: `/deputy-head/staff` (division-scoped — client-side filter, since `GET /staff` has no server-side `division` param, matching its existing open-read-to-any-authenticated-user posture; read-only, no Register/Deactivate) and `/teacher/students` (scoped to the union of classes the teacher class-teaches or subject-teaches, merged from per-class enrollment rosters since `GET /students` has no teacher-scoping param either). Both got real sidebar nav entries (`role-config.ts`).

Bugs found and fixed during manual, per-role browser verification (Teacher, DeputyHead, Admin, Accountant), all confirmed via reasoning about the actual code path before fixing:
- `GuardianTab.tsx` (nested inside `StudentDetail`) had its own mutation UI (add/unlink/relation-edit/create-login) with zero role gating — missed when `StudentDetail`'s own header buttons were correctly gated for the new read-only Teacher case. Added a `canEdit` prop threaded down from `StudentDetail`.
- `SubjectExpertiseCard.tsx`/`QualificationsCard.tsx` (Admin-hardcoded, `RequireAdmin`-gated mutation endpoints) would have shown Add/Save buttons that always 403 on the new read-only DeputyHead staff page — proactively gated with a new `canManage` prop before it was even manually observed.
- A Server→Client Component crash ("Functions cannot be passed directly to Client Components") from passing a `studentHref` function prop into `AttendanceSheet` from a Server Component page — fixed by passing a plain `studentBasePath` string instead, with the href built inline client-side.
- A pre-existing, unrelated bug hit while testing the new read-only staff page: `SubjectExpertiseCard.tsx` called `useSubjects({ size: 200 })`, but `GET /subjects` caps `size` at 100 — a 422 that silently stuck the card on "Loading…" forever. Fixed to `size: 100`, matching the existing correct call site in `ClassDetail.tsx`.
- A same-page re-focus bug in all three `?focus=` components: `openId`'s `useState<string | null>(focusId)` initializer only runs once at mount, so clicking a second search result while already on the destination page (no full remount) scrolled to the new card but never opened/highlighted it. Fixed by deriving `openId` from `focusId` during render (the codebase's established `getDerivedStateFromProps` replacement pattern, already used in `SubjectExpertiseCard.tsx`) instead of an effect — also required by a stricter lint rule (`react-hooks/set-state-in-effect`) against calling `setState` synchronously inside `useEffect`.

Backend: `ruff check`/`ruff format --check`/`mypy`/`pytest` all clean (873 passed, same 2 pre-existing unrelated SMTP-config failures); new tests for both permission widenings and all 3 new search domains across every role. Frontend: `tsc`/lint/Vitest/build all clean. Manually verified in-browser across all 5 roles: Teacher (search → student detail, attendance roster links, read-only Guardian/Health tabs, new Students list page), DeputyHead (search → staff detail, dashboard "Staff in Division" links, new Staff list page, `?focus=` on lesson-plans/schemes), Admin (search across all 6 domains except fee-items, `?focus=` re-navigation from an already-open page), Accountant (fee-items-only search scoping confirmed via passing backend test; UI unaffected by this PR's changes). See `docs/superpowers/specs/2026-07-14-search-navigation-revisit-design.md`.

## Teacher classes page N+1 fix — ✅ Done

Fast follow-up to the dashboard-enrichment PR's `classTeacherId` filter on `GET /classes`, which fixed the Teacher dashboard's N+1 class lookup but deliberately left `/teacher/classes`'s identical pattern untouched as a scoped-out follow-up. Repointed it now: `teacher/classes/page.tsx` used to call `api.classes.teachers.list(c.id)` once per class in the *entire school* just to detect which ones this teacher class-teaches. Replaced with a single `api.classes.list({ classTeacherId })` call (same filter, no new backend work), unioned with the existing subject-teacher class IDs from `classSubjects.listByTeacher`. `SchoolClass.classTeachers` is set to `[]` for these entries since `TeacherClassesView` never actually renders that field — it only reads `isClassTeacher`, `subjectsTaught`, and `studentCount`, all already available without a teacher-name lookup.

`tsc`/lint/Vitest/build all clean (no backend changes, so no new pytest run needed — the filter itself was already tested when it shipped). Manually verified in-browser: the same teacher account shows identical Class-Teacher/Subject-Teacher classification on `/teacher/classes` as it does on the dashboard Overview.

## Dashboard data enrichment/validation — ✅ Done

Closed a Phase 6 backlog item the user flagged directly, with no further detail beyond the name. A full audit of all four role dashboards (Admin, DeputyHead, Teacher, Parent) surfaced one real, currently-shipping bug plus a consistent pattern across every dashboard: the backend already computes richer data than the Overview pages ever display.

**The bug**: `apps/api/app/features/reports/repository.py`'s `_ATTENDANCE_AT_SCHOOL` set used lowercase `{"present", "late"}` to filter attendance records, but the DB stores capitalized values (`AttendanceStatus = Literal["Present", "Absent", "Late", "Excused"]`, a plain `varchar` with no DB enum). Since Postgres string comparison is case-sensitive, this silently zeroed out every "last 7 days attendance" bar app-wide (`/admin/reports`, `/deputy-head/reports`, `/teacher/reports`) — undetected because the reports test suite's own seed fixture used the same wrong lowercase casing, so tests were self-consistently wrong rather than failing. Fixed the filter to `{"Present", "Late"}`, fixed the fixture to match real data, and added the first real assertion on the actual present/total counts (previously only list-length was checked).

**Admin dashboard**: `ReportsService.get_school_stats` already computed `lessonPlans` and `todayAttendance` counts that the Overview never read. Added two new stat cards ("Today's Attendance", "Pending Lesson Plans") wiring them in, and replaced three hardcoded/fake trend strings (`"+3 this term"`, `"Fully staffed"`, a redundant division list) with real computed values (`activeStudents/students`, `activeStaff/staff`, subject count) — all from data already in hand, no new backend work.

**Deputy Head dashboard**: same underused-data pattern — `get_division_stats` already returns `lessonPlans` and `attendanceLast7`, discarded before reaching the Overview. The "Staff Attendance" card was boolean-only ("submitted today: yes/no") despite the page already fetching the full session with every staff member's status; changed to a real `present/total` count. Added a "Pending Lesson Plans" card and replaced a hardcoded "Active members" trend with a real student:staff ratio.

**Teacher dashboard**: added a new `classTeacherId` filter to `GET /classes` (repository joins `class_teachers`) to eliminate a real N+1 — both the Overview and `/teacher/classes` previously fetched teacher assignments for *every class in the school* just to find the ones the current teacher owns (an explicit code comment acknowledged the gap). Only the Overview was repointed to the new filter in this PR; `/teacher/classes`'s identical pattern is a deliberate follow-up, not touched here. Also replaced the "Lesson Plans" card's literal placeholder (`value: null`, hardcoded "My Plans →" text) with real pending/rejected counts from `GET /lesson-plans?teacherId=...` (already supported that filter).

**Parent dashboard**: the "Announcements" stat showed `announcements.length` off a response capped at `size: 4` — any school with 4+ announcements ever showed a permanent, wrong "4"; fixed to use the response's real `total` field. Attendance percentage was computed for `linkedChildren[0]` only, silently ignoring every other child for a multi-child parent; now computed per-child and averaged, with the trend text changing to "Avg across N children" so it's clear what's being shown. Also aligned Parent's "at school" definition (previously strict `"present"` only) to include `"late"`, matching every other dashboard's definition of the same concept.

Backend: `pytest` full suite green (862 passed, same 2 pre-existing unrelated SMTP-config failures); new regression tests for the casing fix and the `classTeacherId` filter. Frontend: `tsc`/lint/Vitest/build all clean; manually verified all four dashboards in-browser with real data (Admin's 6 stat cards, Deputy Head's real student ratio + pending lesson plans, Teacher's fixed class list + lesson-plan counts, Parent's averaged multi-child attendance % and correct announcement total). See `docs/superpowers/specs/2026-07-14-dashboard-data-enrichment-design.md`.

## Parent-facing fee receipts — ✅ Done

Closed the last remaining Phase 5/6 fee-management gap. Originally brainstormed as a system-generated, sequentially-numbered PDF receipt, but the actual intent was simpler and more direct: `FeePayment.receipt_file_urls` (whatever proof-of-payment the Accountant already uploads — a photo of a paper receipt, a MoMo screenshot) already existed, but a prior, deliberate design decision (`ParentFeePaymentRead`'s docstring) explicitly excluded it from the parent-facing view: "the Accountant's proof-of-payment, not the parent's document." Reversed that exclusion instead of building a parallel PDF pipeline — the Accountant's uploaded file *is* the parent's receipt.

Backend: `ParentFeePaymentRead` gained `receiptFileUrls`; `FeesService.my_children_fees`'s mapping now passes the field through, mirroring the Accountant-side `_payment_read`. Frontend: `ParentFeePayment`/`toParentFeePayment` widened to match; the parent fees page now renders a `ClientDocumentDownloadLink` per uploaded file next to each payment — reusing the same generic, already-shared signed-URL download mechanism used for leave documents, scheme resources, and staff/student documents (no new endpoint, no new server action). New pytest coverage confirms the round-trip; manually verified end-to-end in-browser: uploaded a file as Accountant, downloaded the identical file as that student's parent via a real Supabase signed URL.

Drive-by fixes, flagged mid-testing while reviewing this PR — a broader look at the whole Accountant section against the Staff/Classes reference pages turned up four real layout gaps, all fixed together:
- The Fee Items and Balances tables were missing the standard `bg-card border border-border/60 rounded-xl p-4` wrapper every other list page (Classes, Staff) uses around its `DataTable` — fixed in `FeeItemsTable.tsx` and `LearnerFeesTable.tsx` (shared by both Balances and the fee-item detail page, so one fix covers both; `LearnerFeesTable` gained a `bare` prop for callers that supply their own `Card` wrapper instead).
- Fee Items and Balances had no `StatCard` summary row at all, unlike every other list page — added Total/Active/Inactive/This-Year to Fee Items and Total/Outstanding/Paid/Waived to Balances. Balances now fetches the full unfiltered set once and filters client-side (mirroring `StaffTable`'s pattern) so the stat counts stay accurate regardless of which status the dropdown is showing.
- The fee-item detail page (`FeeItemRoster.tsx`) had a redundant hand-rolled "Back to fee items" link — every other detail page (Staff, Class) relies solely on the shell breadcrumb, which this page already wires up via `useBreadcrumbLabel`. Removed the dead link and the now-unused `backHref` prop.
- The same page's roster table sat bare under the header with no section title — wrapped it in a `Card` with an "Assigned Roster (N)" title + live count badge, matching `ClassDetail.tsx`'s "Student Roster (N)" pattern.

See `docs/superpowers/specs/2026-07-14-fee-receipts-design.md`.

## First-time-setup onboarding checklist — ✅ Done

Closed the last item from Phase 6's original checklist (`v2/UHAS_Migration_Execution_Plan.md`). A pre-design audit found no onboarding/setup-guidance UI existed anywhere — a new school's Admin had no ordered path through the real dependency chain (school identity → academic year/terms → classes → staff logins) beyond unguided use of ordinary CRUD pages.

Added a persistent, auto-hiding dashboard widget rather than a one-time wizard or a separate page — it reflects live data on every load, so there's nothing to keep in sync or dismiss. Five checks, in dependency order: **Identity** (`schools.logo_url IS NOT NULL` — a real logo was uploaded, distinct from the frontend's `?? "/logo.png"` display fallback, which never touches the database), **Grading** (raw stored `schools.grading_bands IS NOT NULL` — the *unresolved* column, since `GET /school` always resolves this to GES defaults for display; the check only passes once an Admin actually saves the Grading tab, even unchanged), **Academic calendar** (all 3 `school_terms` rows exist for the current year), **Classes** (at least one class exists for the current year), and **Staff** (at least one non-Admin staff member has a provisioned login).

Backend: new `GET /school/onboarding-status` (any authenticated role; only the Admin UI renders it), computed by `SchoolsService.get_onboarding_status()` — reuses `ClassesRepository`/`SchoolTermsRepository` plus one new `UsersRepository.has_non_admin_staff_login()` query. No new migration — every check reads existing columns. 7 new pytest cases (bare-school all-false, each check independently true, and the combined `allDone` flag), full suite green. Frontend: new `OnboardingChecklist` Server Component at the top of the Admin dashboard — a `Card` with an accent-colored left border, "N of 5 done" header, and a "Go to [X]" link per incomplete step; renders `null` once every check passes. Fixing those links surfaced a real gap in `SettingsPage.tsx`: its `<Tabs>` had no way to open on a specific tab, so a `?tab=grading`-style link would always land on Identity regardless — fixed by reading the `tab` query param via `useSearchParams()` to seed `defaultValue`, falling back to `identity` for an unknown/missing value.

`tsc`/lint/Vitest/build all clean; manually verified in-browser against the seeded demo school (4 of 5 done, only Identity outstanding since the seed script never sets `logo_url`) — confirmed the "Go to Identity" link, confirmed `?tab=calendar` opens the Calendar tab directly, and confirmed the widget disappears entirely once `logo_url` is set and reappears when cleared. See `docs/superpowers/specs/2026-07-14-onboarding-checklist-design.md`.

## Academic-year / term management deep-dive — ✅ Done

Closed the last backlog item from the pre-go-live gap audit's original six. A pre-design audit found four gaps: `apps/web/src/lib/academic-year.ts`'s hardcoded 4-entry `ACADEMIC_YEARS` array (enforced via `z.enum()` on class/exam creation and the year switcher) meant an Admin could no longer create anything for a new year — or even have the app recognize its own `school.academicYear` — once the real year advanced past the array's last entry, since `getCurrentAcademicYear()`'s fallback silently reverted to a hardcoded default; rolling to a new year was two disconnected manual steps (Promotions creates next-year enrolments, but never touches `schools.academic_year`, and an Admin had to separately hand-edit Settings with no validation); `schools.current_term` was purely cosmetic despite the original spec calling for a date-based auto-pick; and the parent dashboard's attendance-percentage calculation hardcoded the academic year as Sept 1–Aug 31 while report cards correctly resolved real `school_terms` dates for the same purpose.

Fixed all four as one pass:
- **Dynamic year source**: deleted the hardcoded array. Every year picker now derives its options from `school_terms` data plus the school's real current + next year (`next_academic_year()`, already shared with Promotions) — an Admin can always start preparing next year even before its `school_terms` rows exist. `getCurrentAcademicYear()` now trusts `school.academicYear` unconditionally instead of falling back to a stale default.
- **Rollover workflow**: two new Admin-only endpoints on the Settings → Calendar tab. **Prepare next year** copies the current year's classes forward (idempotent — skips ones that already exist) and pre-fills next year's `school_terms` by shifting this year's dates forward one year. **Activate next year** flips `schools.academic_year`, guarded to refuse while the current year's promotion season is still open, and audit-logged (`SCHOOL_YEAR_ACTIVATED`).
- **Term auto-pick with override**: new nullable `schools.current_term_override` column. A shared resolver (`app/features/schools/term_resolver.py`) computes the effective current term as override → whichever `school_terms` row's dates contain today → nearest term by date → the legacy stored value, in that order — never erroring even with incomplete data. The Calendar tab's term control is now "Auto — Term N" with an explicit pin.
- **Two downstream bug fixes**: the parent dashboard's attendance-percentage date range now resolves real `school_terms` dates instead of the Sept–Aug guess; exam creation defaults its `term` field to the resolved current term instead of always `1`.

Backend: new migration (`current_term_override`), `PrepareNextYearRead`/`SchoolUpdate` schema changes, 18 new pytest cases (6 pure resolver tests, 6 service-level rollover tests, plus existing suite updates) — all passing alongside the full existing suite. Frontend: `tsc`/lint/Vitest/build all clean; manually verified in-browser that Prepare creates real classes+terms, Activate correctly disables until preparation is done, the year switcher picks up the newly-created year with no code change, and a new exam's term field defaults to the resolved term. See `docs/superpowers/specs/2026-07-14-academic-year-term-management-design.md`.

Code review during this PR caught the attendance class-picker showing a raw class UUID instead of its name after selection — a Base UI `SelectValue` quirk (it doesn't automatically display the matching `SelectItem`'s children; it needs an explicit render-prop function, or the item's `value` and display text must already be identical) already fixed once on the Calendar tab's own new term-override selector earlier in this same PR. A full sweep for the same pattern across every `<Select>` in the app found **11 total instances**: the attendance class picker (`AdminAttendancePicker.tsx`), the "assign cover" substitute-staff picker (`LeaveRequestList.tsx`), fee-item and appointment class/teacher pickers (`FeeItemForm.tsx`, `ParentAppointmentsView.tsx`), the class-detail subject picker (`ClassDetail.tsx`), student registration's class picker (`StudentRegistrationForm.tsx`), both remaining audit-log filters (`AuditLogFilters.tsx`'s User and Target dropdowns), the exam-type picker (`ExamsManager.tsx`), the staff system-role picker (`StaffRegistrationForm.tsx`), the leave-type picker (`LeaveRequestForm.tsx`), and the fee-balances status filter (`BalancesTable.tsx`) — all fixed with the same render-prop pattern.

## Tier 4 follow-on bug sweep — ✅ Done

Six items the user flagged in one batch (staff email edit vs. Auth login, UUID leaks across the frontend, the Profile page's unclear "Documents" card, staff phone-login policy, broken self-service profile-photo upload, and a Settings-page button gap), each investigated independently via parallel research agents before any fix landed, so real bugs and non-bugs weren't conflated.

- **Staff email edit vs. Supabase Auth login** — investigated, found already safe: `StaffService.update` (and the equivalent `GuardiansService.update`) syncs an admin-driven email/phone change straight to Supabase Auth via the admin API (`update_user_by_id(..., email_confirm=True)`) in the same request, by deliberate design (admin edits are trusted, same as account creation) — structurally different from, but not buggy relative to, the self-service link-based confirmation flow. No code change needed.
- **Raw UUIDs leaking into the UI** — one shared root cause and several one-offs. `AutoBreadcrumb.tsx` had no way to resolve a dynamic route segment (e.g. a class/student/staff id) to its real name, so it silently fell back to rendering the raw id — fixed with a new `apps/web/src/features/shell/breadcrumb-context.tsx` (`BreadcrumbLabelProvider` + `useBreadcrumbLabel`) that lets any page feed the breadcrumb the entity's name once fetched; wired into all ~15 affected dynamic routes (attendance, classes, examinations, promotions, staff, students, report cards, assignments, class-reports, lesson plans, schemes, fee items). Also fixed: the attendance roster and the printable student ID card rendered `student.id` instead of `student.slug`; the parent dashboard's "My Children" list, the global search command palette's Students/Staff results, and the PSC report's "Staff ID" column all lacked a display-id field entirely and needed one plumbed through (the PSC fix required a small backend schema addition, `PscStaffEntry.slug`, plus a regenerated `api.d.ts`).
- **Profile page's "Documents" card** — not a bug, just unlabeled: it's a plain card appended to the bottom of the Profile tab (not a real tab), with no `CardDescription` and a generic "No documents uploaded yet." empty state, making it read as confusing dead UI even though the access-control and data plumbing all work correctly (an Admin's own profile does resolve via their own `staff` row). Fixed with real explanatory copy and a clearer empty state, differentiated by `canManage`.
- **Staff phone-login** — investigated and found to be a real, unintended gap: `LoginForm.tsx`'s `classify()` routes purely by input shape (phone-looking string → OTP flow), with no role check anywhere in the pipeline, so a staff account with a confirmed phone could complete phone-OTP login today, contradicting CLAUDE.md's documented "staff = email+password only, phone-OTP is Parent-only" design — and staff never got the onboarding SMS notice guardians do, since that path only fires when there's no email on file (staff always have one). Fixed by adding an explicit role check in `handleVerifyOtp`: phone-OTP now signs out and rejects any non-Parent role immediately after verification, with a clear error message. Code review caught a follow-on inconsistency: the Profile page's phone-field copy ("...before it becomes login-usable") was still shown identically to every role, contradicting the restriction just added — `PhoneChangeCard` gained a `usedForLogin` prop (`true` only for Parent) so staff see accurate copy ("used for contact and SMS notifications only — not for signing in") instead.
- **Self-service profile-photo upload** — two real, distinct bugs, found via a dedicated investigation plus a broader app-wide upload-mechanism audit (which confirmed every OTHER upload site — staff/student registration, StaffDetail/StudentDetail edits, Settings' branding logo — shares one working, unaffected code path). Bug A: an Admin/staff account provisioned without a linked `staff` row (possible via the generic "Add user" flow) silently lost the entire upload widget, since `canEditPhoto` requires a truthy `linkedId`. Bug B: `get-my-photo.ts` checked `linkedId.startsWith("STAFF-")`, but `linkedId` is a UUID, not a `STAFF-XXX` string — always false, so the sidebar/header avatar never showed a real photo for anyone, regardless of what was saved. Given both bugs and that `UserAvatar`'s initials fallback already renders everywhere reliably, removed self-service photo upload entirely (Profile page, Sidebar, Header) rather than fixing a two-bug feature nobody could actually see working — `apps/web/src/features/profile/queries/get-my-photo.ts` deleted, `userPhotoUrl` prop chain removed from `DashboardLayout`/`Sidebar`/`Header`, all four profile route pages simplified. Admin-set staff/student photos (registration forms, tables, printable ID cards) are untouched — a separate, working mechanism.
- **Settings page buttons** — confirmed via git history that `features/settings/` was never touched by the action-button-consistency pass (tier 3); all 6 tabs' Save buttons still used the pre-pass `variant="ink"`. Swapped to `variant="brand"` to match every other primary CTA in the app.

One bug found and fixed along the way, unrelated to the six flagged items: the new `breadcrumb-context.tsx`'s first draft caused an infinite render loop (`useBreadcrumbLabel`'s effect depended on the context value object itself, which was a new object identity every render since the Provider's `value={{...}}` wasn't memoized) — fixed by wrapping `setLabel`/`clearLabel` in `useCallback` and the context value in `useMemo`.

## Action-button + page-layout consistency pass — ✅ Done

A pragmatic consistency fix (not the full UI revamp, which stays separate future work), grown mid-implementation from a series of the user's own follow-on asks. Two new `buttonVariants`: `brand` (the one canonical primary-CTA style — brand green — replacing a mix of hand-rolled classNames, `outline`, `ink`, and plain-`default` across ~45 files, including converting 3 raw hand-styled `<Link>` CTAs on Classes/Staff/Students to real `<Button render={<Link/>}>` per the shadcn-only rule) and `destructive-solid` (replacing ~15 files' copy-pasted `AlertDialogAction` className override, plus fixing 5 destructive actions that had either no red treatment or a third hardcoded red). Also: leave-requests page layout reconciled with other admin list pages; audit-log filter row unwrapped; login/reset-password pages reconciled behind a new shared `AuthBrandPanel` component; UUID sweep off the staff-registration success dialog; footer "v1" removed. Two follow-on bug sweeps this surfaced: (1) a recurring Base UI "changing uncontrolled Select to controlled" console warning, root-caused and fixed across 10 forms (a `<Select value={field.value}>` starting `undefined` on first render, then becoming a defined string once picked); (2) the "Add guardian" dialog's broken tab layout, root-caused to `components/ui/tabs.tsx` itself — its `data-horizontal:`/`group-data-vertical/tabs:` Tailwind variants checked for a literal `data-horizontal`/`data-vertical` attribute that never existed (Base UI actually sets `data-orientation="horizontal"`), so every other `<Tabs>` caller in the app was silently relying on a manual `className="flex flex-col"` override to compensate — this was the one caller that didn't. Fixed the primitive itself rather than adding a fourth workaround. See `docs/superpowers/specs/2026-07-13-action-button-consistency-design.md`.

## Account emails: real provider + branded invite/reset/change — ✅ Done

Staff onboarding had no way to actually send a login invite — `StaffRegistrationForm.tsx`'s success dialog showed a hardcoded placeholder link. Fixed alongside a bigger gap the same investigation surfaced: `provision_login` (shared by staff and guardian logins) relied on Supabase's own generic invite email, off-brand and disconnected from the branded system built in slices 1–4; the same was true for password recovery and email-change confirmation, both triggered directly client-side (`supabase.auth.resetPasswordForEmail`/`updateUser({email})`) with zero backend involvement. All three now mint their Supabase link via the admin `generate_link` API (invite/recovery/`email_change_current`+`email_change_new`) and send it through our own Jinja2 templates instead of Supabase's mailer — verified against the local Supabase CLI stack, including that `generate_link` really does support secure dual-confirmation email-change and that a non-existent email really does raise a distinguishable `NotFoundError`. That last point is load-bearing for `POST /auth/reset-password` (new, public, IP-rate-limited): every branch — unknown email, cooldown active (new `users.last_password_reset_sent_at`, 5 min), Supabase itself reporting no such user — returns the identical 204 so the endpoint can't be used to enumerate accounts, mirroring the anti-enumeration guarantee `resetPasswordForEmail` already gave for free. New `POST /staff/{id}/login` mirrors the existing `POST /guardians/{id}/login` (infers role from the staff row's own `system_role`, no re-asking) and replaces the dead placeholder link with a real "Send invite" action. Production email provider is now Resend (`ResendEmailProvider`, takes precedence in `get_email_provider()`); local dev is Mailpit (`docker compose up mailpit`, view at `:8025`) — `SmtpEmailProvider`'s `user`/`password` became optional so the same class serves both an authenticated real SMTP server and Mailpit's no-auth catcher. See `docs/superpowers/specs/2026-07-13-account-emails-provider-design.md`.

## Pre-go-live gap audit — tier 1 cleanup — ✅ Done

A 65-agent audit workflow swept the codebase for orphaned routes, unwired backend constants, dead code, hardcoded values, and comment rot ahead of go-live. Tier 1 batched the cheap, high-signal, no-design-decision fixes: wired 2 fully-built-but-unreachable pages (`/admin/subjects` into the Admin nav; the per-student report-card page linked from the student detail page's academic tab); deleted confirmed-dead code re-verified with a fresh grep immediately before removal (the superseded `components/dashboard/` directory, 3 unused client hooks, 3 unused backend repository methods, the never-reachable `'button'` variant on `ClientDocumentDownloadLink`/`DocumentDownloadLink` — every call site in the codebase passed `variant="inline"` explicitly); deduped `StaffTable`/`StaffDetail`'s identical `ROLE_LABEL`/`ROLE_PILL`/`ROLE_AVATAR` maps into a shared `features/staff/role-styles.ts`. Remaining audit findings (unwired enum values, endpoints with no frontend consumer, response fields never read) are tracked as backlog, to be folded into whichever PR naturally touches that area.

## 6 item 8 slice 5 — Admin leave management page — ✅ Done

Fifth and last of the initiative, a follow-up PR tracked from slice 3. Built `/admin/leave` by reusing `LeaveRequestList` unchanged (no role-specific logic in it — `GET /leave-requests` already scopes Admin-sees-everyone vs Deputy-Head-sees-own-division server-side, so the frontend just calls the same endpoint with no extra params) plus one new optional `scopeDescription` prop for the empty-state copy. Scope grew mid-PR: researching the Admin gap surfaced a bigger one — none of Admin, Deputy Head, or Teacher's sidebar nav configs (`apps/web/src/features/shell/role-config.ts`) had a "Leave" entry at all, so `/deputy-head/leave` and `/teacher/leave` (both already fully built) had been completely unreachable through the UI since they shipped. Fixed all three roles' nav in this PR. Verified end-to-end in a real browser across all three roles with live seeded data — nav entries render, Admin sees both divisions' requests, Deputy Head (JHS) correctly sees only their own, Teacher's submit-and-view flow works, zero console errors.

## 6 item 8 slice 4 — Attendance absence notifications — ✅ Done

Fourth of the originally-scoped 4-PR initiative. `AttendanceService.upsert_session` is session-based — it deletes and re-inserts an entire class roster on every save, no per-student mark/correct method exists — so the core problem this PR solves is dedup: fetches the previous session's records into a `{student_id: status}` map before the delete, and only a genuine status *transition into* `"Absent"` notifies (a same-day resubmission that leaves an already-absent student unchanged stays silent; `Absent → Present → Absent` genuinely re-notifies). Recipient resolution follows the results-published/fee-reminder precedent: primary guardian only, batched — a guardian with two newly-absent children in the same session save gets one combined email + SMS, not two. `"Late"`/`"Excused"` stay silent; only `"Absent"` triggers. Single-direction (parent-facing only) — one preference pair, `{email,sms}_on_attendance_absent`. The one deliberate default flip in this initiative: `schools.notification_defaults.on_attendance_absent` defaults to `false`, not `true` like every other toggle — attendance is marked daily for potentially every student, a materially higher volume and more sensitive category than the occasional appointments/leave/results events, so a school opts in explicitly rather than this firing unannounced. See `docs/superpowers/specs/2026-07-12-attendance-absence-notifications-design.md`.

## 6 item 8 slice 3 — Leave request notifications — ✅ Done

Third of the 4-PR initiative. Wired the two already-reserved but never-referenced `NotificationKind`s (`LEAVE_REQUEST_SUBMITTED`/`_DECIDED`) into `LeaveRequestsService`: submitting a leave request fans out to every eligible approver — every Deputy Head of the requester's division plus every Admin, both simultaneously eligible (unlike lesson plans' staged Unit-Head-then-Deputy-Head chain) — via `resolve_audience()` merged across `StaffByDivisionAudience(roles=[DeputyHead])` and `AllAdminsAudience()`; each resolved approver gets its own in-app + email + SMS notification (per-recipient, not batched). Approving/rejecting notifies the requester, structurally identical to the appointments `respond` flow. Two new preference directions (`{email,sms}_on_leave_{activity,decided}`) — the first prefs `ProfilePage.tsx`'s `NotificationsTab` has ever shown Admin or Deputy Head (both previously fell through to "nothing to configure," despite being staff who can submit their own leave too). Cancel and substitute-assignment stay silent by explicit scope decision. Surfaced a real gap: the backend already lets Admin approve/reject leave, but no `/admin/leave` page exists in the frontend — only `/deputy-head/leave` and `/teacher/leave` do, so Admin's email CTA falls back to `/admin/staff` for now. Building a real `/admin/leave` page is tracked as a follow-up PR, planned last in this initiative (after attendance-absence notifications). See `docs/superpowers/specs/2026-07-12-leave-request-notifications-design.md`.

## 6 item 8 slice 2 — Appointment notifications + HTML email templates — ✅ Done

Second of the 4-PR initiative. Closed `appointments.cancel`'s zero-notification bug (now in-app + email + SMS, matching `create`/`respond`) and built this codebase's first HTML email infrastructure: `apps/api/app/integrations/email/templates/` (Jinja2 `Environment`, a shared `base.html` every content template extends) retrofits the two pre-existing plain-text-only jobs (lesson-plan-rejection, results-published) with HTML alongside their existing plain-text fallback, plus 3 new appointment email jobs. Two preference **directions** (not per-event-type): teacher-facing "appointment activity" (`create` + `cancel`) and parent-facing "appointment decided" (`respond`) — new `user_preferences.{email,sms}_on_appointment_{activity,decided}` columns (this codebase's first per-user SMS preferences) plus matching `schools.notification_defaults` toggles, same two-tier gate as lesson-plan-rejection. User design review caught two real issues after the first pass: brand colors were a generic guess instead of the project's actual `--brand`/`--accent-teal` tokens (now correct), and the header banner was "too much for an email" — replaced with a plain citrus-accent top border plus a proper footer (school name/address/contact email, and a "Manage email preferences" link into the recipient's own profile tab, since there's no real unsubscribe mechanism). Email body font is a system sans-serif stack, not the app's actual `next/font/google` webfont (unloadable in most email clients, Outlook especially) and not the PDF report card's Georgia serif. Drive-by fix: `email_on_results_published` existed as a column since the report-card-polish PR but was never exposed through `/me` — Parents had zero UI to opt out; now wired through alongside the new appointment prefs. `ProfilePage.tsx`'s `NotificationsTab` restructured from a hard `user.role === TEACHER` ternary to a per-role preference-row list (Parent previously saw "nothing to configure for your role yet"). See `docs/superpowers/specs/2026-07-12-appointment-notifications-design.md`.

## 6 item 8 slice 1 — Auth contact-info fixes — ✅ Done

First of a 4-PR "close the email/SMS gaps app-wide" initiative — a follow-up audit found the gap was much bigger than the original "appointments" framing (most domain events have in-app only; `announcements`' "email a copy" toggle doesn't exist in code at all; `appointments.cancel` has zero notification; `results_published`'s email opt-out pref has no frontend UI; a generic `sms/jobs/sms_fanout.py` fan-out job sat unused). This slice fixes matching phone AND email desync bugs: editing either anywhere in the app updated only the local `guardians`/`staff` mirror column, never Supabase Auth's own value — OTP login / password login silently kept authenticating against the *old* value after a "successful" change. Self-service phone goes through Supabase's `updateUser({phone})` → `verifyOtp({type:"phone_change"})`, then `POST /me/phone/confirm` mirrors back only what Supabase confirmed; self-service email goes through Supabase's link-based `updateUser({email})` confirmation (no inline code to pair with), with `POST /me/email/confirm` called best-effort on every profile-page load to self-heal once the link is clicked. Admin-driven edits (phone or email) sync directly, trusted, no challenge — same as account creation. Also: phone-only accounts (the common Parent case) got zero notice their account exists — now emits an unconditional onboarding SMS via the existing (previously unused) fan-out job; new `app/core/phone.py` Ghana-format normalizer applied to every guardian/staff phone write; switched the active SMS provider to Arkesel (`ArkeselSmsProvider`, precedence over Hubtel, falls back to it if only Hubtel is configured); added "this is also your login" UI notes to the Profile phone/email fields and the Admin guardian/staff creation forms, so an Admin doesn't assume a phone/email typed there is just a contact record. See `docs/superpowers/specs/2026-07-12-auth-contact-info-fixes-design.md`.

## 6 item 5 — Report card polish — ✅ Done

A ground-truth audit found all five backlog sub-items genuinely 0% built, though two had partial groundwork: free-text `class_teacher_remark`/`head_of_school_comment` already existed (no structured conduct/co-curricular fields); `subject_position` (class rank) already existed (no class-average). New on `student_report_remarks`: `kg_observations` (JSONB, 5 fixed developmental domains, KG-only — `ReportCardService.get` skips the numeric score table entirely for `division == KG`) and `conduct_ratings`/`interests_co_curricular` (every division). Class-average is a same-class-and-exam `AVG(total_score)` query joined into each `ReportCardScoreRow`. Batch print resurrects the dormant, placeholder-only Inngest job pair (`reports/jobs/report_generate.py`/`report_batch.py`, deleted) as a real `exams/jobs/report_card_batch.py` — renders every student's PDF via the same content-hash-cached single-student renderer, zips them, and tracks status on a new `report_card_batch_jobs` table (async — the admin polls `GET .../report-cards/batch` for a signed download URL). Email-to-parent on publish wires the previously dead `RESULTS_PUBLISHED` notification kind: `ExamsService.set_published` resolves every student with scores/observations for that exam, creates one in-app notification per child, and (gated by the school's `notification_defaults.on_results_published` + a new per-user `email_on_results_published` preference, same two-tier gate as lesson-plan-rejection) emits one batched email per **primary guardian** listing all their newly-published children. See `docs/superpowers/specs/2026-07-12-report-card-polish-design.md`.

## 6 item 3 — Leave management depth — ✅ Done

A third distinct audit outcome — half right, half wrong: leave types + the request/approve workflow already existed, but balances, documents, and substitute cover were genuinely 0% built. The audit also surfaced three unrelated bugs, fixed in the same PR by explicit direction: a Deputy Head division-scope leak (any Deputy Head could view/approve/reject leave for staff in *any* division, despite a code comment falsely claiming otherwise — now enforced via the same `_assert_can_view_student`-style staff-division lookup pattern used elsewhere); a rejection-reason field that was collected in the UI and silently discarded, never sent to the backend; and no audit-log write on approve/reject (`LEAVE_DECIDED` action added). New: `leave_requests.document_urls` (bare JSONB array — always requester-uploaded at creation time, no labelled-child-table ambiguity like `student_documents`); `leave_requests.substitute_staff_id` (simple informational annotation, not a schedule override); Casual-leave balance computed on the fly (`schools.casual_leave_annual_days`, admin-configurable via new Settings "Leave" tab, minus the inclusive day-count of that staff member's approved Casual requests so far in the current UTC calendar year — deliberately not a maintained counter, so it can't drift). Only Casual-style leave gets a balance; the other six leave types don't draw against a quota. See `docs/superpowers/specs/2026-07-12-leave-management-depth-design.md`.

## 6 item 4 — Staff profile depth — ✅ Done

Unlike the student-profile audit, this backlog item was accurately scoped — genuinely ~0% built (no `hire_date`, qualifications, subject-expertise link, or staff documents beyond `photo_url`). Added `staff.hire_date`; a `staff_subject_expertise` join table (open read, Admin-only full-replace `PUT` — a simple tag list, distinct from `class_subjects.teacher_id`'s current-assignment meaning); a `staff_qualifications` child table (open read, Admin-only add/remove); a `staff_documents` child table mirroring `student_documents`, but gated tighter than this feature's usual open-read precedent — `GET /staff/{id}/documents` is Admin-any-or-self-only. New "Qualifications" tab on the Admin staff-detail page; a read-only "My Documents" section on the self-service `/profile` page. Also fixed an unrelated pre-existing flaky test (`test_school_stats_admin` — a fixture computed "today" in local machine time instead of UTC, disagreeing with `ReportsService._today()` for ~2 hours a day). See `docs/superpowers/specs/2026-07-10-staff-profile-depth-design.md`.

## 6 item 7 — "Built by SimplifydLabs" attribution — ✅ Done

Shared `BuiltByAttribution` component (`apps/web/src/components/`), linked to simplifydlabs.com, used in the login-page footer and the dashboard sidebar footer — no existing dashboard-wide footer/about page to hook into, so the persistent sidebar chrome was the natural spot.

## 6 item 2 — Audit log filters — ✅ Done

`audit_log` already had indexes on `user_id` and `(target_table, target_id)` — no migration needed. Added `userId`/`targetTable`/`targetId` query params to `GET /audit-log` (mirrors the existing `action`/date-range pattern), a new `GET /audit-log/actors` (distinct actors who've actually appeared in this school's log, not the full staff/guardian directory — populates the user-filter dropdown), and a new `GET /audit-log/export` (CSV of every matching row, unpaginated, first CSV-export precedent in this codebase). Admin-only throughout, same as the existing list endpoint.

## 6 item 1 — Student profile depth — ✅ Done

A pre-design audit found siblings + all-guardians display were already ~90% built (the only gap: `list_siblings` was Admin/Deputy-only — closed via the same parent-bypass `list_guardians` already had, no schema changes). Medical info (`blood_type`, `medical_notes`, `emergency_contact_name/phone`) and student documents (`student_documents` child table — labelled + accountable-uploader, not a bare JSONB array) were genuinely new. Both get their own gated endpoints rather than folding into `StudentRead`/`GET /students/{id}`, once implementation surfaced that endpoint has no role/ownership gate at all today — embedding sensitive fields there would have leaked them to every authenticated user in the school. Medical: viewable by Admin/Deputy(own division)/Teacher(teaches the class, new `_assert_can_view_medical` gate)/own-parent; editable by Admin or the student's own parent only. Documents: viewable by Admin/Deputy/own-parent; uploaded/deleted by Admin only — matching this feature's existing Admin-only-mutation precedent (Deputy already couldn't edit guardians or core student fields either). New `/parent/children/[id]` detail page (siblings, medical, documents) alongside the existing list page. See `docs/superpowers/specs/2026-07-09-student-profile-depth-design.md`.

## 5 Slice 3 — Fee reminder SMS — ✅ Done

Real `HubtelSmsProvider` (Quick Send API, HTTP Basic auth, `respx`-mocked tests — no live Hubtel account yet, config-gated with a stub fallback exactly like the email integration). This codebase's first `inngest.TriggerCron` job (every prior job is event-triggered) and first "sweep every school" job: weekly (Mondays 07:00), texts + in-app-notifies each overdue fee's primary guardian, one message per guardian even with several overdue fees. No on-demand "send now" button — rejected as abuseable. `learner_fees.last_reminder_sent_at` (6-day idempotency cooldown) surfaces on the Accountant dashboard and balances table as "last reminded." Phase 5 complete. See `docs/superpowers/specs/2026-07-09-fee-reminder-sms-design.md`.

## 5 Slice 2 — Parent fee view — ✅ Done

`GET /fees/my-children` reuses the existing `StudentsService.list_for_guardian` ownership pattern (no new resolution logic) to return a Parent's own children's fee balances + payment history, with response schemas (`Parent*Read`) deliberately narrower than the Accountant-facing ones — no recorder identity, no receipt-file paths. `/parent/fees`: per-child total owed/outstanding plus an itemized fee breakdown with payment history, read-only, a pure Server Component (no client JS). See `docs/superpowers/specs/2026-07-09-parent-fee-view-design.md`.

## 5 Slice 1 — Fee tracking core — ✅ Done

New `fees` feature: `fee_items` (catalog, scoped school/division/class) → `learner_fees` (one row per learner, soft-deletable) → `fee_payments` (Accountant-recorded, multi-file receipt uploads — no receipt generation). `RequireAccountant` dep. Bulk-assign a fee item to its scope's roster with individual edit/waive/exclude after. Accountant `/accountant` dashboard (balance/collected/overdue/active-items summary), `/accountant/fee-items` (list + create), `/accountant/fee-items/[id]` (roster + assign + record payment), `/accountant/balances` (school-wide, status-filterable). Decision gate closed: parents will not pay online, so no payment-gateway/`payment_gateway_events` work — bursar-collected only. Service-layer auth, consistent with every other domain (no RLS yet — tracked as a future hardening slice). See `docs/superpowers/specs/2026-07-09-fee-tracking-core-design.md`. Parent fee view + SMS reminders are separate follow-on slices.

## Rate-limiting audit — ✅ Done

No login/OTP endpoint exists in FastAPI at all — Supabase Auth handles that client-side — so the original "protect login" framing didn't apply; every route requires a verified JWT except `/health`. Added `slowapi`: a global 300/min-per-user default plus a stricter 10/min limit on the report-card PDF endpoint (the one route with a real cost profile). Keyed by authenticated user id from the JWT, not client IP — sidesteps `uvicorn` not being configured to trust Railway's `X-Forwarded-For`. In-memory storage today (correct for the current single Railway instance); `REDIS_URL` is wired for whenever `apps/api` scales to multiple replicas. Part of Phase 3.5.

## Real report-card PDF rendering — ✅ Done

`GET /students/{id}/report-card/pdf` renders the existing report-card template (Jinja2 port of `ReportCard.tsx`) to real PDF bytes via WeasyPrint, cached in Supabase Storage keyed by a content-hash of the assembled data (publish status doesn't actually lock scores/remarks, so caching couldn't key off that). `apps/api` now builds via its own `Dockerfile` (WeasyPrint's system libraries) instead of the `railpack` builder — Railway prefers a service's Dockerfile automatically, no `railway.toml` changes needed. Batch/bulk printing remains separate, deferred work. Part of Phase 3.5.

## Drop JHS class streams — ✅ Done

School runs one class per level — no streams. Renamed `class-jhs1a/2a/3a` → `class-jhs1/2/3` and `"JHS 1A/2A/3A"` → `"JHS 1/2/3"` across seed + tests + UI. Deleted the now-dead `stripSuffix`/`streamSuffix` helpers and the three stream-specific tests; tightened the JHS-3-graduates check from `startsWith("JHS 3")` to `=== "JHS 3"`.

## Admin Settings page — ✅ Done

`/admin/settings` (Identity / Calendar / Grading / Communication / Security / Branding tabs) turned out to already be fully built from earlier work — an audit found every tab real and wired to the `schools` row, not the stale pre-migration stub the spec assumed. Remaining gap was narrower: `grading_bands`/`score_weights` were correctly consumed by score computation server-side already, but the score-entry live preview and the report-card/PDF grading-key legend still hardcoded the GES defaults — now both read the school's real resolved bands/weights. `session_timeout_minutes` was removed outright (unenforceable — Supabase Auth controls session expiry, not this app); `password_min_length`/`force_password_change_on_first_login` are now read-only in the UI since neither is wired to real enforcement yet. Part of Phase 3.5.

## Profile page completion — ✅ Done

Every tab is real now: Save Changes (display name + phone), Notification preferences, self-service deactivation, Active Sessions ("sign out other devices"), and **2FA/TOTP** (Supabase Auth MFA — enrol from Profile, login-time challenge, un-bypassable proxy `/verify-2fa` gate, admin `reset-mfa` for lockout recovery); photo upload + password change already were. Completes Phase 3.5.

## Strategy A Migration (Phases 0–3) — ✅ Done

Replaced this table's entire Next.js-only architecture: FastAPI + SQLAlchemy/Alembic backend (`apps/api/`) is now the sole data-access + mutation path — Drizzle and Next.js Server Action mutations are fully decommissioned. Supabase replaced Firebase for both Auth and Storage. Phase 0: FastAPI skeleton + Supabase CLI/Alembic baseline. Phase 1: Supabase Auth (JWT `app_metadata` roles, `proxy.ts` routing). Phase 2: full DB cutover — every feature ported from Drizzle/Server Actions to FastAPI routers + SQLAlchemy. Phase 3: Inngest background jobs, Supabase Storage, SMS domain (Hubtel stubbed), email ported to Python. See [`v2/UHAS_Migration_Execution_Plan.md`](v2/UHAS_Migration_Execution_Plan.md) for phase-by-phase detail and [apps/api/README.md](apps/api/README.md) for current backend status.

## Outbound email — ✅ Done — moved to Python

Was `src/lib/email.ts` (nodemailer); ported 1:1 to `apps/api/app/integrations/email/` in Phase 3 and wired into the lesson-plan-rejection Inngest job. The old `apps/web/src/lib/email.ts` file is unused dead code now (nothing imports it) — not yet deleted.

## 8 — Testing (layer 3) — ⚠️ Disabled since Strategy A migration

Playwright E2E (7 tests, 5 specs) targeted the pre-migration Firebase-auth + Server-Action surface and fails against the current Supabase + FastAPI stack. The CI job is wired but skipped (`if: false` in `.github/workflows/ci.yml`) rather than deleted. Re-porting the suite to the new auth flow + API client is tracked but not scheduled.

## CI workflow — ✅ Done (superseded — see below)

Original `apps/web`-only CI: Postgres 16 service container, lint → tsc → tests → build, dummy Firebase env placeholders. Superseded by the two-job (`web` + `api`) workflow described in the Strategy A Migration row below.

## 8 — Testing (layers 1 + 2) — ✅ Done

Vitest set up with a separate `uhas_sms_test` Postgres. 128 tests across 10 files (~12 s end-to-end). **Layer 1 (unit, no DB)** covers `computeGrade` / `computeTotalScore` / `assignSubjectPositions` / `computeAggregate`, `computePromotionSuggestion`, `autoPickTargetClass`, `nextAcademicYear`. **Layer 2 (integration, real DB)** covers auth (login, role redirect, mustChangePassword, change-password), students (create + transfer + audit), scores (save + compute + rerank + `SCORE_OVERRIDE` audit), promotions (full transaction: close + Active/Repeating/Withdraw + `PROMOTION_APPROVED` audit), attendance (save + leave-request lifecycle), audit-log helper + viewer queries. Tests caught one real bug: `saveScoresAction` looked up existing rows by a constructed ID that never matched the seed's IDs — now fixed. Scripts: `npm run db:test:setup` (one-shot, creates DB + migrates), `npm test`, `npm run test:watch`.

## Theme default + UX polish — ✅ Done

UHAS brand palette is now the default — root `<html data-color-scheme="uhas">` so it applies on first paint with no flash. `useTheme().setColorScheme("default")` still removes it. "Mark all present" is now a one-click action on both student and staff attendance sheets — stages everyone as present (keeping approved-leave staff on leave) and immediately saves.

## File uploads (originally Firebase Storage) — ✅ Done, backend later swapped

Reusable `ImageUploadField` / `FileUploadField` / `DocumentDownloadLink` / `UserAvatar` on student + staff + own-profile photos and lesson-plan/scheme/assignment attachments. Originally backed by the Firebase Storage emulator (`storage.rules`, port 9199); the storage backend itself moved to Supabase Storage (`photos` public, `documents` signed URLs) in Phase 3 — `apps/web/firebase.json` and `storage.rules` are now unused leftovers, not yet deleted.

## Audit log viewer — ✅ Done

Admin-only `/admin/audit-log`. Filters by action + date range (default last 30 days), pagination 50/page. Expandable rows show side-by-side before/after JSON with changed-key highlighting.

## DB Cutover (mock → Drizzle) — ✅ Done, later superseded

Removed `USE_MOCK_DATA` and the entire `src/lib/mock/` directory. Every action and query went through Drizzle at this point — Drizzle itself was later removed in the Strategy A migration (see below); FastAPI + SQLAlchemy is the current data-access layer. `DB_DRIVER` env var picks `pg` for Docker/Railway or `neon-http` for Neon prod (auto-detects from `*.neon.tech` host). Generated baseline migration; `npm run db:migrate && npm run db:seed` brings up a fresh Postgres with the same demo data as before. Audit log wired for the four sensitive admin mutations. See `docs/superpowers/specs/2026-05-19-db-cutover-design.md`.

## 5.7 — Student Promotion — ✅ Done

Year-end promotion workflow. After DB cutover, approval materialises real `enrollments` rows (Active for Promote, Repeating for Repeat), flips `students.isActive=false` for Withdraw, and writes one `PROMOTION_APPROVED` audit log row in a single transaction.

## 7c — Academic Calendar — ✅ Done

New `calendar_events` table + actions. Admin `/admin/calendar` adds/deletes events (term start/end, exam, holiday, event). Deputy Head, Teacher, Parent all see a read-only `/<role>/calendar` view with Upcoming and Past sections.

## 7b — PSC Report — ✅ Done

Admin `/admin/reports/psc` renders the printable Population & Staff Census: school totals, per-class boy/girl breakdown with division subtotals, school total, teachers grouped per division with Unit Head flag. Reuses the report-card print mode at A4.

## 7a — Reports dashboards — ✅ Done

New `features/reports` with stat queries per scope. Admin `/admin/reports`: school totals, gender breakdown, per-division population bars, lesson-plan workflow distribution, exam status, today's attendance progress. Deputy Head `/deputy-head/reports`: division-scoped stats, 7-day attendance, lesson-plan funnel, class ranking by aggregate. Teacher `/teacher/reports`: per-class attendance + subject averages.

## 6b — Appointments — ✅ Done

New `appointments` table + feature. Parent `/parent/appointments`: child + teacher picker (teachers derived from child's class subject assignments and class-teacher junction), preferred date/slot, reason. Teacher `/teacher/appointments`: pending inbox with Confirm / Decline (decline requires a reason). Status: pending / confirmed / declined / cancelled; parent can cancel pending requests.

## 6a — Announcements — ✅ Done

New `features/announcements`. Audience = `all` \| `division:<D>` \| `class:<classId>`. Admin (`/admin/announcements`) posts to any audience and can delete any. Deputy Head (`/deputy-head/announcements`) scoped to their division. Parent (`/parent/announcements`) sees school-wide + announcements matching any linked child's division/class. Critical-flag badge surfaces everywhere.

## 5c — Assignments — ✅ Done

New `assignments` table. Teacher `/teacher/assignments`: list + create/edit + Publish/Unpublish/Delete with class/subject pickers tied to teacher's assigned subjects. Parent `/parent/assignments`: aggregates published assignments across all linked children's classes; shows due-date status (overdue / due today / upcoming), per-child attribution, attachment links.

## 5b — Schemes of Work / Learning — ✅ Done

New `schemes` table (type: `work` \| `learning`, structured `content` and/or `fileUrl`). Teacher `/teacher/schemes`: list + create/edit form with tab toggle between "Write from system" and "Upload URL". Submit to Head of School. Admin `/admin/schemes`: queue of pending submissions, expand to preview, optional comment + Acknowledge.

## 5a — Lesson Plans — ✅ Done

Teacher `/teacher/lesson-plans`: list, create, edit, delete, submit. Structured form (topic, learning objectives, teaching methods, resources, assessment plan, optional attachment URL). Approval chain: Teacher submits → Unit Head approves at `/teacher/reviews` → Deputy Head approves at `/deputy-head/lesson-plans` → status = approved. Reject with required comment at either stage; teacher edits drop back to draft. Status pill: draft / submitted / unit-head-approved / approved / rejected.

## 4c — Workflow — ✅ Done

New tables `class_report_submissions` + `student_report_remarks`. Class Teacher `/teacher/class-reports`: per-(exam × class) page with one textarea per student for class-teacher remarks; Save draft + Submit to Head of School. Admin `/admin/examinations/[examId]/review`: list of classes with submission status; per-class review page shows each student's class-teacher remark + a textarea for Head of School's comment (per-student save). Report card now renders both remark + comment rows. Publishing locks all remarks/comments.

## 4b — Report Card — ✅ Done

Server-rendered, browser-printable report card (`#report-card-print-area`, A4) matching the school template — logo placeholders, header, student info, Core/Elective subject tables with score/position/grade/interpretation, attendance, signatures, grading-scale legend, motto. Parent route `/parent/results/[studentId]/[examId]` (published only). Admin route `/admin/students/[id]/report-card/[examId]` (any exam, with unpublished notice).

## 4a — Score Entry — ✅ Done

Schema: scores columns for cat1/cat2/projectWork/groupWork; helpers for total/grade/position/aggregate. Admin: examinations list with create + publish/unpublish. Teacher: examinations landing + score entry grid (Mid-Term = raw 100, End-of-Term = 60% exam + 4×10% components placeholder), auto-computed total/grade, locked when exam published.

## 3.5 — Model Reconciliation — ✅ Done

Schema and mocks updated to match user feedback: division split (KG / Lower Primary / Upper Primary / JHS); HOD removed, Unit Head added as a flag on staff with conditional dashboard nav; multiple class teachers per class (junction); staff UHAS ID; student middle name; school-specific grading scale (Highest..Lowest, 1–9); attendance bulk "Mark all present" and required late-reason.

## 3 — Attendance — ✅ Done

Student daily attendance (teacher + admin), staff attendance + leave requests (deputy head), parent attendance calendar view. Live attendance stats on Teacher, Deputy Head, and Parent dashboards. All on mock data.

## 2d — Classes & Subjects — ✅ Done

Class list + create (fixed names), subject list + create, class detail with subjects/teacher assignment + student roster. All on mock data.

## 2c — Staff Management — ✅ Done

Staff list (Admin-scoped), registration form, role assignment, staff detail + edit + deactivate/reactivate. All on mock data.

## 2b — Student Detail & ID Card — ✅ Done

Student detail view, edit profile, class transfer (with confirmation), printable ID card (browser print + @media print CSS)

## 2a — Student Records — ✅ Done

Student list (Admin + Deputy Head scoped), registration form, soft-deactivate/reactivate, division + status filter pills

## 1 — Auth & User Management — ✅ Done

Login, role routing, change-password, reset-password (wired to Firebase `sendPasswordResetEmail`), admin user management UI (stats, DataTable, invite flow), dashboard shell (Sidebar, Header, profile page, academic year switcher, search, notifications, dark mode toggle). Non-admin dashboards (Deputy Head, Teacher, Parent) with live attendance stats. Session expiry warning modal: a `SessionExpiryWatcher` in `DashboardLayout` reads the `session_expires_at` cookie and shows an AlertDialog 5 min before expiry with a live countdown + Extend / Sign out buttons (Extend re-issues all session cookies for another 8h).

## 0 — Foundation — ✅ Done

DB schema, Firebase emulator, mock fixtures, middleware, folder structure

